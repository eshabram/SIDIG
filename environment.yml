# environment.yml
name: sidig
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pytorch
  - torchvision
  - pytorch-cuda=12.1
  - pip
  - pip:
    - diffusers
    - transformers
    - accelerate
    - safetensors
    - peft
    - pillow
    - tqdm
